---
# Production inventory - list all production hosts here
all:
  children:
    linux_servers:
      hosts:
        # Add production servers below
        your_server
        
    monitoring_servers:
      hosts:
        # Add monitoring servers below (if any)
